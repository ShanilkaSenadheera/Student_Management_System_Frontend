import { StudentPaymentInfo } from './student-payment-info';

describe('StudentPaymentInfo', () => {
  it('should create an instance', () => {
    expect(new StudentPaymentInfo()).toBeTruthy();
  });
});
